import tensorflow as tf

def create_model():
    model = tf.keras.Sequential([
        # Add your sequential layers here, for example:
        # tf.keras.layers.Embedding(...),
        # tf.keras.layers.LSTM(...),
        # tf.keras.layers.Dense(...),
    ])

    return model

